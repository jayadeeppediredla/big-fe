function intersection(a,b){
    let count ={};
    // keep count of every of element in the array in a;
    let result =[

    ];
    // keeps tracks of intersecton of element in a and b ;
    for(const x of a){
        count[x]

    }
}